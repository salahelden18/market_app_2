import 'package:flutter/material.dart';

class OrderPaymentItemWidget extends StatelessWidget {
  const OrderPaymentItemWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
